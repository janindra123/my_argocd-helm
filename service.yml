---
apiVersion: v1
kind: Service
metadata:
  name: multi-port-service
spec:
  selector:
    app: bunny
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: https
      port: 6379
      targetPort: 6379
  type: LoadBalancer
